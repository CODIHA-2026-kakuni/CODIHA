# CONTRIBUTING - GitHub運用ルールブック

CODIHA（千葉市小型家電リサイクル回収ボックス位置表示アプリ）を複数人で開発するにあたっての、GitHub運用ルールです。
全員が同じルールで運用することで、作業の衝突や事故（誤ってmainを壊す等）を防ぎます。

迷ったときは「このルールブック → チームで相談 → ここに追記」の順で更新してください。

## 1. 基本方針

- `main` ブランチは常に動く状態を保つ（壊れたコードを直接pushしない）。
- 作業は必ず**ブランチを切って**行い、`main` へは **Pull Request（PR）経由**でのみ反映する。
- `main` への直接pushは禁止（GitHub側でブランチ保護設定を行うことを推奨。9章参照）。
- 迷ったら小さくコミット・小さくPRを心がける（レビューしやすく、問題の切り分けがしやすい）。

## 2. ブランチ運用

シンプルな **GitHub Flow** を採用します（`main` から都度ブランチを切り、作業が終わったらPRで `main` に戻す方式）。

```text
main ─┬─────────────────────────────▶
      ├─ feat/map-display ──▶ PR ──┘
      ├─ fix/box-icon-position ──▶ PR ──┘
      └─ docs/update-readme ──▶ PR ──┘
```

### 2.1 ブランチの作り方

```bash
git checkout main
git pull origin main          # 最新のmainを取得
git checkout -b feat/xxxx  # 作業ブランチを作成
```

### 2.2 ブランチ命名規則

`種別/内容が分かる短い英語` を基本形とします（日本語ローマ字でも可）。

| 種別 | 用途 | 例 |
| --- | --- | --- |
| `feat/` | 新機能の追加 | `feat/map-display` |
| `fix/` | 不具合修正 | `fix/box-icon-position` |
| `docs/` | ドキュメントのみの変更 | `docs/update-readme` |
| `refactor/` | 挙動を変えないコード整理 | `refactor/api-client` |
| `chore/` | 設定変更・依存関係更新など雑務 | `chore/update-deps` |

- 1ブランチ = 1目的にする（あれもこれも詰め込まない）。
- 作業が長引きそうな場合は、区切れる単位でPRを分割する。

## 3. コミットメッセージ

現状のスタイルを踏襲し、**日本語・自由記述**とします。ただし読みやすさのため以下は守ってください。

- 「〜しました」「〜を追加」など、**何をしたか**が一目でわかる文にする。
  - 良い例: `地図上に回収ボックスのアイコンを表示する機能を追加`
  - 良い例: `README.mdの誤字を修正`
  - 避けたい例: `修正`, `update`, `wip`
- 1コミット = 1つのまとまった変更にする（無関係な変更を1コミットに混ぜない）。
- 途中経過の一時保存コミット（`wip` 等）は、PRを出す前に `git rebase -i` 等で整理するか、そのままでも構わないが**PRの説明欄で経緯を補足する**。

## 4. Pull Request（PR）

### 4.1 PRを出すタイミング

- 作業がキリの良いところまで進んだら、**Draft PR**（まだレビュー不要の下書きPR）を早めに出してもOK。他の人が進捗を把握しやすくなる。
- レビューしてほしい状態になったら、Draftを解除して通常のPRにする。

### 4.2 PRに書く内容

タイトルと説明欄には、最低限以下を書く。

- **何を変更したか**（概要）
- **なぜ必要か**（背景・関連Issue番号があれば `#12` のように記載）
- **動作確認の方法・結果**（スクリーンショットや実行結果があれば添付）
- 影響範囲や注意点（あれば）

### 4.3 レビュー・マージのルール

- PRを出した本人は**自分のPRを自分でApproveしない**（セルフマージ禁止）。
- マージ前に**最低1人以上**のレビューでApproveをもらう。
- レビュー依頼を受けたら、できるだけ**24時間以内**に目を通す（すぐ着手できない場合はその旨だけでもコメントする）。
- マージ方法は **Squash and merge**（複数コミットを1つにまとめてmainに反映）を基本とする。履歴がすっきりし、mainのコミットログが読みやすくなるため。
- マージ後、不要になった作業ブランチは削除する（GitHubの「Delete branch」ボタンでOK）。

### 4.4 コンフリクトが起きたら

```bash
git checkout main
git pull origin main
git checkout feat/xxxx
git merge main        # または git rebase main
# コンフリクトを解消してコミット
git push
```

迷ったら一人で抱え込まず、関係するコードを書いた人に相談する。

## 5. コードレビューの観点

レビューする側は、以下の視点で確認する。

- 仕様通りに動くか（可能ならプルしてローカルで動作確認する）。
- 読んで理解できるコードか（変数名・関数名が内容と合っているか）。
- 秘密情報（APIキー・パスワードなど）が誤って含まれていないか。
- 既存の機能を壊していないか。

コメントの付け方の目安:

- 修正必須の指摘は具体的に（「ここが動かない」「〇〇の場合エラーになる」など）。
- 好み・提案レベルの指摘は「nit:」や「(お好みで)」など、必須ではないと分かるようにする。
- 良い実装には積極的に「良い」「参考になる」等のポジティブなコメントも残す。

## 5.5 AIツールによるPRレビューの活用

人間のレビューに加えて、各自が使い慣れたAIツール（Claude Code / ChatGPT / Gemini CLI など）でPRの事前レビューを行ってよい。AIレビューは**人間のレビューの代わりにはならない**（4.3の「最低1人以上のApprove」ルールは変わらず必須）が、事前にAIでチェックしておくことで、人間レビュアーの負担を減らせる。

### 5.5.1 使うツールは各自の判断でよい

- どのAIツールを使うかは統一しない。各自が使い慣れたもの・契約しているものを使ってよい。
  - 例: Claude Code（CLI／エディタ拡張）、ChatGPT（Web／CLI連携）、Gemini CLI、その他エディタのAIレビュー機能など。
- チームで特定のツールを強制する必要はない。ただし後述の「投稿ルール」は全員共通で守る。

### 5.5.2 やり方（例：使うCLIごとのコマンド）

共通の流れは「`gh pr diff` でPRの差分を取得する → AIツールに渡す → 観点を指示する」。
以下は各AI CLIでの実行例（`42` はPR番号）。事前に各CLIのインストール・ログイン（`gh auth login` / 各AIツールの認証）が必要。

観点は5章のレビュー観点に合わせて統一する:

```text
1. 仕様通りに動くか
2. 読んで理解できるコードか（変数名・関数名が内容と合っているか）
3. 秘密情報（APIキー・パスワード等）が含まれていないか
4. 既存の機能を壊していないか
指摘は「修正必須」と「nit（好み・提案レベル）」に分けること
```

**Claude Code CLI を使う場合**

```bash
# 対話モードで起動し、その場でPR差分を読ませる
claude
# セッション内で「gh pr diff 42 を実行して、上記の観点でレビューして」と指示する

# もしくは非対話（ワンショット）で実行する場合
gh pr diff 42 > /tmp/pr42.diff
claude -p "以下のPR差分をレビューしてください。観点は仕様適合・可読性・秘密情報混入・既存機能への影響の4点。指摘は「修正必須」と「nit」に分けて。

$(cat /tmp/pr42.diff)"
```

**ChatGPT（Codex CLI）を使う場合**

```bash
gh pr diff 42 | codex exec "このPR差分をレビューしてください。観点は仕様適合・可読性・秘密情報混入・既存機能への影響の4点。指摘は「修正必須」と「nit」に分けて。"
```

**Gemini CLI を使う場合**

```bash
gh pr diff 42 | gemini -p "このPR差分をレビューしてください。観点は仕様適合・可読性・秘密情報混入・既存機能への影響の4点。指摘は「修正必須」と「nit」に分けて。"
```

**補足**

- 差分だけでは背景が伝わらず誤検知が増えることがあるため、必要に応じて `gh pr view 42 --json title,body` などでPRの説明文も一緒に渡すと精度が上がる。
- レビュアー側が使う場合は、取得した指摘をそのままGitHubに貼らず、5.5.3のルールに従って自分で確認してから投稿する。

### 5.5.3 AIの指摘をPRに投稿する場合のルール

- AIの指摘をそのままコピペしない。**必ず自分で内容を確認し、妥当と判断したものだけ**をコメントとして投稿する（AIは誤った指摘をすることがあるため）。
- AI由来の指摘だと分かるように、コメントの先頭に `[AI] ` や `(AIレビューより)` 等を付けて区別する。
  - 例: `[AI] nit: この関数名は処理内容と少しずれている気がします（AIの提案: renameToXxx）`
- 秘密情報（APIキー・トークン・個人情報等）を外部のAIサービスに貼り付けない。特にChatGPT/Gemini等の外部Web UIに、`.env` の中身や認証情報を含むコードを貼らないこと。
- AIレビューはあくまで補助。5章の「コードレビューの観点」（仕様通り動くか、秘密情報が含まれていないか等）は、最終的に人間が確認する。

### 5.5.4 セキュリティ・利用範囲の注意

- 各自の契約・利用規約の範囲でAIツールを使うこと（会社/組織のアカウントで使う場合は、その利用ポリシーに従う）。
- 本リポジトリのコードやIssue内容を外部AIサービスに送信すること自体に問題がないか、迷った場合はチームに確認する。

## 6. Issue の使い方

- バグ報告・新機能提案・タスク管理はすべて **GitHub Issue** に起票する（口頭・チャットのみで済ませない）。
- 作業を始める前にIssueを立て、作業するブランチ・PRとIssueを `#番号` で紐付ける。
- ラベル運用の例（必要に応じて追加・調整してよい）:
  - `bug`：不具合
  - `enhancement`：新機能・改善
  - `documentation`：ドキュメント関連
  - `question`：相談・確認事項
- 着手する人が決まったら、自分をAssigneeに設定する。

## 7. 秘密情報・環境変数の扱い

- APIキー・パスワード等は**絶対にコミットしない**。`.env` 等に分離し、`.gitignore` で除外する（現状の `.gitignore` で `.env` 系は除外済み）。
- 誤ってコミットしてしまった場合は、単に削除するコミットを積むだけでは履歴に残るため、**すぐにチームに共有**し、該当キーの無効化・再発行と履歴からの除去を行う。

## 8. リリース・タグ運用（必要になったら）

現時点では常時 `main` を最新として運用する。今後リリース単位を区切りたくなったら、以下を追加検討する。

- 節目ごとに `git tag v0.1.0` のようにタグを打つ。
- 必要に応じて `release/` ブランチやGitHub Releasesページを活用する。

## 9. このルールブックについて

- ルールは一度決めたら終わりではなく、運用しながら**チームで見直してよい**。
- 変更したいときは、このファイル自体をPRで更新し、チームでレビューして合意を取ってからマージする。

## 10. AIエージェントが自律作業する場合

Claude / OpenAI / Google系のAIがこのリポジトリ上で自律的に作業する場合は、本ファイルに加えて、各ツールが自動で読み込む以下のルールブックにも従う（禁止事項・評価方法を規定）。

- Claude系: `CLAUDE.md`
- OpenAI系: `AGENTS.md`
- Google系: `GEMINI.md`
